/**
 * Optics package barrel — exposes the stable pure-engine surface for external imports.
 *
 * Kept intentionally thin so callers can import from src/optics without learning the
 * internal module layout.
 */

export * from "./compat.js";
export * from "./types.js";
